const Employee = require("./Employee");

class Manager extends Employee {
    constructor(arg1, arg2) {
        const arg1 = x;
        const arg2 = y;

        super(position, identifier, email, credential);
        this.arg1 = arg1;
        this.arg2 = arg2;
    }
}

const manager = new Manager(param1, param2);
manager.printInfo();