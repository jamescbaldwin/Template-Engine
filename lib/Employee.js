class Employee {
    constructor(position, identifier, email, credential) {
        this.position = position;
        this.identifier = identifier;
        this.email = email;
        this.credential = credential;
    }
}